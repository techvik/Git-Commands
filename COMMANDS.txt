git log //lists all the commits
git diff <log_id> <log_id> //find differences between to commits or files
git diff //compares working dir and staging area
git diff --staged //compares staging area and repo
git clone <url> //clone a repo to ur pc
git checkout <log_id> //go to a particular commit
git init <repo_address> //initialize a repo
git add <file_name> //add to staging area
git reset <file_name> //removed from staging area
git commit //commits
git commit -m <message> //commits with the given message
git checkout master //to come back
git branch //lists all the branches
git branch <branch_name> //creates a new branch
git checkout <branch_name> //to go the branch mentioned
git log --graph --oneline master coins //compares two branches
git show <id> //compare with parent
git branch -d <branch_name> //deletes the branch
git merge <b1> <b2> //merges branches
git remote //lists all remotes
git remote add origin <link> //adds a remote
git remote -v
git push origin master //push changes to github
git pull origin master //to pull from github to pc
git
git fetch origin //update the local repo

